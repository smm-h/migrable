# Single-file example

A basic migrable setup with one config file and two migrations.

    cd examples/single-file
    migrable status
    migrable migrate --dry-run
    migrable migrate
    migrable migrate --rollback
