version: 2

builds:
  - main: .
    binary: migrable
    ldflags:
      - -s -w -X main.Version={{.Version}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - docs/man/*.1

release:
  extra_files:
    - glob: dist/conformance-tests.tar.gz

checksum:
  name_template: "checksums.txt"
